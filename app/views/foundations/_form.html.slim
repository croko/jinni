= simple_form_for @foundation, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    '
    = link_to t('.cancel', :default => t("helpers.links.cancel")), foundations_path, :class => 'btn'
